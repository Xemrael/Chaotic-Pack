# Planned - No ETA

- Start using AutoModPack to control the mods and configs, instead of a whole pack reinstall
- Add RAM Warning for modpack
- Keep default options for modpack - If AutoModPack can't remove mod or config, providing the changed config and mods folder instead of a whole pack import maybe be a solution to prevent user set options loss
- Adding server to selection screen
